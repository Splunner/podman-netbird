[Unit]
Description={{postgres_db.description}}
After=network-online.target
Wants=network-online.target

[Container]
Image={{postgres_db.image}}
Network={{postgres_db.network}}

EnvironmentFile={{postgres_db.env_file}}
Environment=PGDATA=/var/lib/postgresql/data/data

{% for volume  in netbird_server.volumes %}
{% set Volume = volume %}
Volume={{ Volume }}
{% endfor %}


{% for port in postgres_db.ports %}
{% set PublishPort = port %}
PublishPort={{ PublishPort }}
{% endfor %}

ContainerName={{postgres_db.container_name}}

HealthCmd=pg_isready -U {{postgres_db.postgres_db_username}}
HealthInterval=10s
HealthTimeout=5s
HealthRetries=5
HealthStartPeriod=20s

[Service]
Restart=on-failure
TimeoutStartSec=180
TimeoutStopSec=60

[Install]
WantedBy=default.target